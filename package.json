{
	"name": "@sigrea/vue",
	"version": "0.7.1",
	"description": "Vue 3 composables for Sigrea molecules and signals.",
	"license": "MIT",
	"type": "module",
	"packageManager": "pnpm@10.0.0",
	"publishConfig": {
		"access": "public"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/sigrea/vue.git"
	},
	"homepage": "https://github.com/sigrea/vue#readme",
	"bugs": {
		"url": "https://github.com/sigrea/vue/issues"
	},
	"engines": {
		"node": ">=24"
	},
	"sideEffects": false,
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"import": "./dist/index.mjs",
			"require": "./dist/index.cjs"
		}
	},
	"main": "./dist/index.cjs",
	"types": "./dist/index.d.ts",
	"files": ["dist"],
	"keywords": ["signals", "reactivity", "vue", "molecule", "typescript"],
	"scripts": {
		"dev": "vite --config playground/vite.config.ts",
		"build": "unbuild",
		"prepack": "unbuild",
		"smoke": "node --input-type=module -e \"const mod = await import('@sigrea/vue'); for (const key of ['useMolecule', 'useSignal']) { if (typeof mod[key] !== 'function') throw new Error(key + ' export is missing'); }\" && node -e \"const mod = require('@sigrea/vue'); for (const key of ['useMolecule', 'useSignal']) { if (typeof mod[key] !== 'function') throw new Error(key + ' export is missing'); }\"",
		"changelog": "changelogen",
		"test": "vitest run",
		"test:coverage": "vitest --coverage",
		"typecheck": "tsc -p tsconfig.json --noEmit",
		"format": "biome check .",
		"format:fix": "biome check --write .",
		"cicheck": "pnpm -s test && pnpm -s typecheck && pnpm -s build && pnpm -s smoke && pnpm -s format"
	},
	"peerDependencies": {
		"@sigrea/core": "^0.7.0",
		"vue": "^3.4.0"
	},
	"devDependencies": {
		"@biomejs/biome": "1.9.4",
		"@sigrea/core": "^0.7.0",
		"@vitejs/plugin-vue": "^5.1.4",
		"@vitest/coverage-v8": "^3.2.4",
		"@vue/test-utils": "^2.4.0",
		"baseline-browser-mapping": "^2.10.30",
		"changelogen": "^0.6.2",
		"jsdom": "^24.1.3",
		"lefthook": "1.13.6",
		"tsx": "^4.20.5",
		"typescript": "5.9.3",
		"unbuild": "3.6.1",
		"vite": "^5.4.6",
		"vitest": "^3.2.4",
		"vue": "^3.4.0"
	},
	"pnpm": {
		"onlyBuiltDependencies": ["lefthook", "@biomejs/biome"]
	}
}
